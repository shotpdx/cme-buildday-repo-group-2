"""Route module placeholder.

Routes for the D2C home-screen reference app are defined inline inside
``app.build_app`` (see ``app.py``). This module exists as a placeholder for
Task 11, which will extend the UI with HyperFrames motion + HTML rendering
and may refactor routes out of ``app.py`` at that point.
"""
